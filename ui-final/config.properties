# Configuración del Cliente RAFT

# Servidor por defecto (HTTP monitor del main-worker)
server.host=localhost
server.port=8001

# Timeout de conexión (milisegundos)
connection.timeout=10000

# Número máximo de reintentos de conexión
connection.retries=3

# Número máximo de redirecciones al líder
max.redirects=5

# Nivel de log (TRACE, DEBUG, INFO, WARN, ERROR)
log.level=INFO
